
import Position from "./Position";

class Snake {
   private _positions: Position[];

   constructor() {
       this._positions = [
           new Position(0, 2),
           new Position(0, 1),
           new Position(0, 0)
       ]
   }









}

export default Snake;
